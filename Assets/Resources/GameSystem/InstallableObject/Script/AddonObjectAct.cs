using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AddonObjectAct : MonoBehaviour
{
    public bool Tic;

    // Start is called before the first frame update
    void Start()
    {
        Tic = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
